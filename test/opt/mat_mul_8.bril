main {
  a_len: int = const 64;
  a: ptr<int> = alloc a_len;
  b_len: int = const 64;
  b: ptr<int> = alloc b_len;
  c_len: int = const 64;
  c: ptr<int> = alloc c_len;
  one: int = const 1;
  a_ptr: ptr<int> = id a;
  a_val: int = const 62;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 42;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 18;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 8;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 89;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 9;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 54;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 98;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 70;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 27;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 8;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 14;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 19;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 24;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 53;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 73;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 52;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 58;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 15;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 56;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 23;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 73;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 85;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 77;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 64;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 5;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 55;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 19;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 11;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 20;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 97;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 92;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 73;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 28;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 53;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 35;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 83;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 16;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 2;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 14;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 39;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 27;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 66;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 51;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 30;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 67;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 99;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 84;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 55;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 55;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 98;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 47;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 50;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 0;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 0;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 22;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 39;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 76;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 57;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 34;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 100;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 39;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 93;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  a_val: int = const 2;
  store a_ptr a_val;
  a_ptr: ptr<int> = ptradd a_ptr one;
  b_ptr: ptr<int> = id b;
  b_val: int = const 78;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 44;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 39;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 1;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 55;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 73;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 17;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 7;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 52;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 82;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 82;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 40;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 40;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 88;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 78;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 93;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 71;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 99;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 49;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 31;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 35;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 43;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 92;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 83;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 45;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 32;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 34;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 57;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 39;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 12;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 95;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 37;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 28;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 98;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 47;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 99;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 79;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 33;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 61;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 63;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 34;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 35;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 100;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 52;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 94;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 74;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 10;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 47;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 42;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 27;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 32;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 98;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 43;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 94;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 38;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 74;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 55;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 89;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 23;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 98;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 72;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 18;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 39;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  b_val: int = const 31;
  store b_ptr b_val;
  b_ptr: ptr<int> = ptradd b_ptr one;
  c_ptr: ptr<int> = id c;
  c_val: int = const 42;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 93;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 60;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 64;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 8;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 13;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 49;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 33;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 38;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 13;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 27;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 79;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 75;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 89;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 12;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 63;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 2;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 0;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 30;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 9;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 52;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 33;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 85;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 10;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 56;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 51;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 14;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 4;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 90;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 40;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 89;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 66;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 90;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 44;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 98;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 16;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 38;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 90;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 12;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 62;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 72;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 30;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 1;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 80;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 65;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 62;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 38;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 70;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 54;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 85;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 69;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 80;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 61;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 12;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 77;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 19;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 18;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 1;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 98;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 74;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 20;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 22;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 49;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  c_val: int = const 85;
  store c_ptr c_val;
  c_ptr: ptr<int> = ptradd c_ptr one;
  end: int = const 8;
  i: int = const 0;
  one: int = const 1;
for.cond.1:
  v5: bool = lt i end;
  br v5 for.body.1 for.end.1;
for.body.1:
  j: int = const 0;
for.cond.6:
  v10: bool = lt j end;
  br v10 for.body.6 for.end.6;
for.body.6:
  k: int = const 0;
for.cond.11:
  v15: bool = lt k end;
  br v15 for.body.11 for.end.11;
for.body.11:
  a_inc: int = mul i end;
  a_inc: int = add a_inc j;
  a_ptr_idx: ptr<int> = ptradd a a_inc;
  a_val: int = load a_ptr_idx;
  b_inc: int = mul j end;
  b_inc: int = add b_inc k;
  b_ptr_idx: ptr<int> = ptradd b b_inc;
  b_val: int = load b_ptr_idx;
  c_inc: int = mul i end;
  c_inc: int = add c_inc k;
  c_ptr_idx: ptr<int> = ptradd c c_inc;
  c_val: int = load c_ptr_idx;
  tmp: int = mul a_val b_val;
  c_val: int = add c_val tmp;
  store c_ptr_idx c_val;
  k: int = add k one;
  jmp for.cond.11;
for.end.11:
  j: int = add j one;
  jmp for.cond.6;
for.end.6:
  i: int = add i one;
  jmp for.cond.1;
for.end.1:
  c_val: int = load c_ptr_idx;
  print c_val;
  free a;
  free b;
  free c;
}
